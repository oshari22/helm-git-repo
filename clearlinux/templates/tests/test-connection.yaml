apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "clearlinux.fullname" . }}-test-connection"
  labels:
    {{- include "clearlinux.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "clearlinux.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
